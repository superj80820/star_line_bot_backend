const users = require('./users');
const faceInfos = require('./faceInfos');
const faceFaces = require('./faceFaces');
const auth = require('./auth');

module.exports = {
    users,
    faceInfos,
    faceFaces,
    auth,
}